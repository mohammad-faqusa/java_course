## 113. Polymorphism Challenge, Part 2: Finalizing Car Inheritance and Overridden Method

### the process 
1. in previous project `PolymorphismChallenge`
2. lets create the two other subclasses of the car 
3. copy and paste the subclass , and use edit -> find -> replace 
   * change the class name to `ElcetricCar`
   * change the variable `avgPerCharge`
   * cylinders -> batterySize 
4. change the overriden methods 
5. copy to `HybridCar` 
   * copy and paste 
   * change the name 
   * the fields are the same
   * add `batterySize`, add to constructor also, assigned directly 
   * change the overridden  methods 
6. back tom Main method , create the objects declared with car, but with sub classes instances 

