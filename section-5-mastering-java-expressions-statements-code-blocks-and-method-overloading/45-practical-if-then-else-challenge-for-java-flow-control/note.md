## challenge

```java
        score = 10000;
        levelCompleted = 8;
        bonus = 200;

        if(gameOver == true){
            finalScore += (levelCompleted * bonus);
            System.out.println("your final score was " + finalScore);
        }
```
