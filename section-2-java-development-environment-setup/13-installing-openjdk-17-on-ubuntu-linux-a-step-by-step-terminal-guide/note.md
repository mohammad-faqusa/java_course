## 13. Installing OpenJDK 17 on Ubuntu Linux: A Step-by-Step Terminal Guide

### The process 
1. open terminal 
2. check if java is already installed `java -version`
3. type this command  
```terminal
sudo apt update 
sudo apt install -y openjdk-17-jdk
```
4. check if java is installed 

